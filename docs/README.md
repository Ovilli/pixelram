# PixelRAM documentation

The documentation is a Jekyll site using the Just the Docs theme.

To preview it locally:

```sh
cd docs
bundle install
bundle exec jekyll serve
```

The repository's Pages workflow builds this directory and publishes it to GitHub Pages.
